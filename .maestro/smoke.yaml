appId: com.u1.slicer.orca
---
# Smoke test: app launches, shows correct title and version string.
# Mirrors bridge tests/smoke.spec.ts

- launchApp:
    clearState: true

- runFlow:
    when:
      visible: "Android app compatibility"
    commands:
      - tapOn: "Don't show again"
# Title is visible
- assertVisible: "U1 Slicer"

# Prepare landing content is visible
- assertVisible: "Load a 3D Model"
- assertVisible: "Supports STL, 3MF, OBJ, STEP"

# Bottom nav bar has all 5 tabs
- assertVisible: "Prepare"
- assertVisible: "Printer"
- assertVisible: "Preview"
- assertVisible: "Jobs"
- assertVisible: "Settings"

# Navigate to Settings and back
- tapOn: "Settings"
- assertVisible: "Settings"
- tapOn: "Prepare"
- assertVisible: "U1 Slicer"

